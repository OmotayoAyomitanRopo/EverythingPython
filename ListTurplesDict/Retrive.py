#!/usr/bin/env python
def element_at(my_list, idx):
    if idx < 0: # check if element is less than 0
        return none
    if idx >= len(my_list): # Check if elemnt is out of range
        return none
    return my_list[idx] # Returns the element in the list
